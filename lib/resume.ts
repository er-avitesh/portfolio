import data from '@/data/resume.json';

export const resume = data;
export const { basics, experience, domainSkills, skills, publications, certifications, memberships, achievements, metrics, education, languages } = data;
